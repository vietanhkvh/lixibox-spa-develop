export const KEY = Object.freeze({
  enter: 'Enter'
});

export const SIGN_IN_MODAL_DISPLAY_DELAY_MS = 120000;
export const MAINTENANCE_MODE_LONG_POLL_INTERAL_MS = 20000;
export const MAINTENANCE_MODE_TIMEOUT_UPDATE_INTERAL_MS = 1000;
