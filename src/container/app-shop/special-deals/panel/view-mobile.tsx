import { AppShopSepcialSwitchRouting } from '../../../../routings/router';
import WrapLayout from '../../../layout/wrap';

import { IProps } from './model';

const renderView = (props: IProps) => (
  <WrapLayout>
    <AppShopSepcialSwitchRouting />
  </WrapLayout>
);

export default renderView;
