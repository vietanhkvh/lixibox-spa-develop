import FooterMagazine from '../../../../../components/general/footer-information';

import { IProps } from './model';

const renderDesktop = (props: IProps) => {
  return <FooterMagazine />;
};

export default renderDesktop;
