import { IProps, IState } from './model';

export const DEFAULT_PROPS = {} as IProps;
export const INITIAL_STATE = {
  isShowMobileTabModal: 0,
  isShowAddToCartSuccessModal: false,
  selectedProduct: null
} as IState;
