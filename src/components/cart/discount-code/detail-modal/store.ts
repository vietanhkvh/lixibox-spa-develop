import { connect } from 'react-redux';
import DiscountCodeDetailModal from './view';

const mapStateToProps = (state) => ({});
const mapDispatchToProps = (dispatch) => ({});

export default connect<any, any, any>(mapStateToProps, mapDispatchToProps)(DiscountCodeDetailModal);
