import { IFilterBrandProps, IFilterBrandState } from './model';

export const DEFAULT_PROPS = {} as IFilterBrandProps;

export const INITIAL_STATE = {
  showRefreshIcon: false,
  showViewMore: false
} as IFilterBrandState;
