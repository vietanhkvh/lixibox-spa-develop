export interface IProps {
  /**
   * menu browse nodes
   */
  menu: any;
  /**
   * active slug
   */
  slug?: string;
  /**
   * loading phase flag
   */
  isLoading?: boolean;
}

export interface IState {}
